.venv/
.DS_Store
__pycache__/
*.pyc

# Activation caches and scratch notes (per CLAUDE.md)
caches/
notes/

# Saved activation files
*.npz
*.safetensors

# Editor / IDE
.vscode/
.idea/

# Historical: beads tracker was removed during the mechbench migration.
# These entries stay as defense-in-depth against stray state if the tool
# ever reappears — the repo should never contain them on purpose.
.beads/
.dolt/
*.db
.beads-credential-key
